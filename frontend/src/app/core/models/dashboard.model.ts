export interface UserResponse {
  name: string;
  email: string;
  countryCode: string;
  phoneNumber: string;
  address: string;
  accountNumber: string;
  ifscCode: string;
  branch: string;
  accountType: string;
}
